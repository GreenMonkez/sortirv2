{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Mail envoyé{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="alert alert-info" role="alert">
            <p class="mb-3">
                Si un compte correspondant à votre adresse e-mail existe, un e-mail vient d'être envoyé contenant un
                lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
                Ce lien expirera dans
                <strong>{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</strong>.
            </p>
            <p>
                Si vous ne recevez pas d'e-mail, veuillez vérifier votre dossier de spam ou
                <a href="{{ path('app_forgot_password_request') }}" class="alert-link">Réessayer</a>.
            </p>
        </div>
    </div>
{% endblock %}
