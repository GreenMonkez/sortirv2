{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block title %}Edit Sortie{% endblock %}

{% block body %}
    <h1 style="color: #66563C">Modifier une sortie</h1>
    <section class="container my-4" style="background-color: #AEEBE7; padding: 20px; border-radius: 8px;">

        <div data-controller="geo-api"
             data-sortie-id="{{ sortie.id }}"
             data-region-code="{{ region_code|default('') }}"
             data-departement-code="{{ departement_code|default('') }}"
             data-city-name="{{ city_name|default('') }}">
            {{ include('sortie/_form.html.twig', {'button_label': 'Update'}) }}
            <br>
        </div>

        <div style="display: none;" id="delete-form">
            {{ include('sortie/_delete_form.html.twig') }}
        </div>
    </section>
    <script>
        document.getElementById('btn-delete').addEventListener('click', function (event) {
            event.preventDefault(); // Prevent the default action
            let deleteForm = document.getElementById('delete-form');
            if (deleteForm.style.display === 'none') {
                deleteForm.style.display = 'block';
            } else {
                deleteForm.style.display = 'none';
            }
        });
    </script>
{% endblock %}
